Every PR/feature must include tests. Minimum:
- Service functions: unit tests (happy + error paths)
- Server Actions: integration tests
- New pages: basic render test
Skip tests only for pure styling changes.
